apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pronunt-platform-core-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/pronunt/pronunt-helm-charts.git
    targetRevision: test
    path: infra/platform-core
    helm:
      valueFiles:
        - values.yaml
        - values-test.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
